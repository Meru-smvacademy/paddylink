// Placeholder — real design arrives from Figma in a later step.
// Target of every CTA and the sticky button on /buyers.
export default function RegisterBuyerPage() {
  return (
    <main>
      <h1>Register as Buyer</h1>
    </main>
  );
}
