# supabase/migrations

`001_paddylink_schema.sql` belongs in this directory, **verbatim**.

It was not included with the Step 1 brief, so it has not been added and the
migration has not been run. Drop the tested file in here unchanged — do not
edit or regenerate it — then follow "Apply the database migration" in the
project README.
